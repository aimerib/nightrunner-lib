on:
  push:
    branches:
      - main
jobs:
  test:
    uses: ./.github/workflows/test.yml
  deploy:
    needs: test
    uses: ./.github/workflows/deploy.yml
    secrets:
      token: ${{ secrets.NPM_TOKEN }}
